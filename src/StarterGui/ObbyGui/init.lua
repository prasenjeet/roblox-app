-- ObbyGui root module. Rojo maps this folder as a ScreenGui.
-- Child scripts (HUD, FinishScreen) are ModuleScripts that this init
-- creates as UI instances so the folder becomes a proper ScreenGui.

-- When using Rojo the folder becomes a ScreenGui automatically if
-- default.project.json maps it with "$className": "ScreenGui".
-- This init.lua is kept for reference / manual Studio builds.

return {}
